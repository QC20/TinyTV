# Copy to the root of the boot partition before first boot.
# Replace the placeholders below; country is your two-letter ISO code.
country=DK
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

network={
    ssid="YOUR_WIFI_SSID"
    psk="YOUR_WIFI_PASSWORD"
}
