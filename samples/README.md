# Samples

`480x320.mp4` is an encoded clip, kept as a reference for what the converter
output looks like — resolution, bitrate and how it sits on the panel.

It is 480×320, from an earlier build that used a smaller screen. The current
target is 780×480; see [../code/encoding](../code/encoding).
